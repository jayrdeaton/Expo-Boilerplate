import { render } from '@testing-library/react-native'
import React from 'react'

import { AddressPanel } from '../'

describe('AddressPanel', () => {
  it('renders correctly', () => {
    render(<AddressPanel />)
  })
})
