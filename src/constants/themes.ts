import { MD3DarkTheme as PaperDarkTheme, MD3LightTheme as PaperLightTheme } from 'react-native-paper'

const dark = PaperDarkTheme
const light = PaperLightTheme

export const themes = { dark, light }
